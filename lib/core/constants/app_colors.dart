import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary      = Color(0xFF1B3FC4);   // Uniflow blue
  static const primaryLight = Color(0xFF4966D6);
  static const primaryDark  = Color(0xFF0F2A9E);

  // Neutrals (used for surfaces, not grays)
  static const ink900  = Color(0xFF0D0F14);   // near-black text
  static const ink700  = Color(0xFF2C3040);
  static const ink500  = Color(0xFF5C6175);
  static const ink300  = Color(0xFFADB2C3);
  static const ink100  = Color(0xFFEEEFF4);   // very light surface

  // Semantic
  static const success = Color(0xFF1DB97B);
  static const warning = Color(0xFFF59E0B);
  static const danger  = Color(0xFFEF4444);
  static const info    = Color(0xFF3B82F6);

  // Surface
  static const surface     = Color(0xFFFFFFFF);
  static const surfaceWarm = Color(0xFFF8F9FC);
  static const border      = Color(0xFFE8EAF0);
}
